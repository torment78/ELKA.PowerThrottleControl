<UserControl x:Class="ELKA.PowerThrottleControl.NetworkPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource WindowBackgroundBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Padding="14,11" Margin="0,0,0,12" Background="{DynamicResource SurfaceAltBrush}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8">
            <WrapPanel VerticalAlignment="Center">
                <TextBlock VerticalAlignment="Center" Foreground="{DynamicResource SecondaryTextBrush}" Text="VBAN UDP port"/>
                <TextBox x:Name="VbanPortBox" Width="78" Margin="10,0,20,0" Text="6980"/>
                <CheckBox x:Name="PrivateProfileCheckBox" Margin="0,0,16,0" HorizontalAlignment="Left" Content="Private" IsChecked="True"/>
                <CheckBox x:Name="DomainProfileCheckBox" Margin="0,0,16,0" HorizontalAlignment="Left" Content="Domain"/>
                <CheckBox x:Name="PublicProfileCheckBox" HorizontalAlignment="Left" Content="Public (less secure)"/>
            </WrapPanel>
        </Border>

        <WrapPanel Grid.Row="1" Margin="0,0,0,12">
            <Button x:Name="RefreshButton" Click="Refresh_Click" Background="#FF2D68C4" BorderBrush="#FF4B83D5" Content="Detect VB-Audio apps"/>
            <Button x:Name="AllowVbanButton" Click="AllowVban_Click" Background="#FF267A46" BorderBrush="#FF3F9961" Content="Allow VBAN UDP"/>
            <Button x:Name="AllowFullButton" Click="AllowFull_Click" Background="#FFB87824" BorderBrush="#FFD39242" Content="Advanced: allow all traffic"/>
            <Button x:Name="RemoveButton" Click="Remove_Click" Background="#FFB83D48" BorderBrush="#FFD15C66" Content="Remove ELKA rules"/>
            <Button x:Name="ListButton" Click="List_Click" Background="#FF535D6B" BorderBrush="#FF697585" Content="List ELKA rules"/>
        </WrapPanel>

        <Border Grid.Row="2" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6">
            <DataGrid ItemsSource="{Binding Applications, RelativeSource={RelativeSource AncestorType=UserControl}}"
                      Background="{DynamicResource SurfaceBrush}" Foreground="{DynamicResource PrimaryTextBrush}"
                      AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" CanUserReorderColumns="False"
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="{DynamicResource GridLineBrush}"
                      BorderThickness="0" RowHeaderWidth="0" RowHeight="50">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsRunning}" Value="True"><Setter Property="Background" Value="{DynamicResource GreenRowBrush}"/></DataTrigger>
                            <Trigger Property="IsSelected" Value="True"><Setter Property="BorderBrush" Value="{DynamicResource SelectionBrush}"/><Setter Property="BorderThickness" Value="1"/></Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Width="48" Binding="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                        <DataGridCheckBoxColumn.Header><CheckBox x:Name="SelectAllCheckBox" Click="SelectAll_Click" ToolTip="Select or clear all detected applications"/></DataGridCheckBoxColumn.Header>
                    </DataGridCheckBoxColumn>
                    <DataGridTextColumn Header="VB-Audio application" Width="2*" IsReadOnly="True" Binding="{Binding DisplayName}"/>
                    <DataGridTextColumn Header="Architecture" Width="105" IsReadOnly="True" Binding="{Binding Architecture}"/>
                    <DataGridTextColumn Header="State" Width="100" IsReadOnly="True" Binding="{Binding StatusText}"/>
                    <DataGridTextColumn Header="Executable path" Width="4*" IsReadOnly="True" Binding="{Binding ExecutablePath}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <TextBlock x:Name="StatusText" Grid.Row="3" Margin="0,12,0,0" TextWrapping="Wrap"
                   Foreground="{DynamicResource SecondaryTextBrush}"
                   Text="Running VB-Audio applications are highlighted and selected automatically. VBAN defaults to UDP port 6980."/>
    </Grid>
</UserControl>
