<UserControl x:Class="ELKA.PowerThrottleControl.GeneralNetworkPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource WindowBackgroundBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Padding="14,11" Margin="0,0,0,10" Background="{DynamicResource SurfaceAltBrush}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8">
            <StackPanel>
                <WrapPanel Margin="0,0,0,9">
                    <TextBlock VerticalAlignment="Center" Foreground="{DynamicResource SecondaryTextBrush}" Text="Ports"/>
                    <TextBox x:Name="PortsBox" Width="210" Margin="10,0,18,0" Text="80,443"
                             ToolTip="Examples: 80,443   5000-5010   80,443,5000-5010"/>
                    <TextBlock Margin="0,0,8,0" VerticalAlignment="Center" Foreground="{DynamicResource SecondaryTextBrush}" Text="Protocol"/>
                    <RadioButton x:Name="TcpRadio" Margin="0,0,12,0" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryTextBrush}" Content="TCP" GroupName="Protocol"/>
                    <RadioButton x:Name="UdpRadio" Margin="0,0,12,0" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryTextBrush}" Content="UDP" GroupName="Protocol"/>
                    <RadioButton x:Name="BothRadio" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryTextBrush}" Content="Both" GroupName="Protocol" IsChecked="True"/>
                </WrapPanel>
                <WrapPanel>
                    <TextBlock Margin="0,0,8,0" VerticalAlignment="Center" Foreground="{DynamicResource SecondaryTextBrush}" Text="Direction"/>
                    <CheckBox x:Name="InboundCheckBox" Margin="0,0,14,0" HorizontalAlignment="Left" Content="Inbound" IsChecked="True"/>
                    <CheckBox x:Name="OutboundCheckBox" Margin="0,0,22,0" HorizontalAlignment="Left" Content="Outbound" IsChecked="True"/>
                    <TextBlock Margin="0,0,8,0" VerticalAlignment="Center" Foreground="{DynamicResource SecondaryTextBrush}" Text="Profiles"/>
                    <CheckBox x:Name="PrivateProfileCheckBox" Margin="0,0,14,0" HorizontalAlignment="Left" Content="Private" IsChecked="True"/>
                    <CheckBox x:Name="DomainProfileCheckBox" Margin="0,0,14,0" HorizontalAlignment="Left" Content="Domain"/>
                    <CheckBox x:Name="PublicProfileCheckBox" HorizontalAlignment="Left" Content="Public (less secure)"/>
                </WrapPanel>
            </StackPanel>
        </Border>

        <WrapPanel Grid.Row="1" Margin="0,0,0,10">
            <Button x:Name="DiscoverButton" Click="Discover_Click" Background="#FF2D68C4" BorderBrush="#FF4B83D5" Content="Search all applications"/>
            <Button x:Name="AllowPortsButton" Click="AllowPorts_Click" Background="#FF267A46" BorderBrush="#FF3F9961" Content="Allow selected ports"/>
            <Button x:Name="AllowAllButton" Click="AllowAll_Click" Background="#FFB87824" BorderBrush="#FFD39242" Content="Advanced: allow all traffic"/>
            <Button x:Name="RemoveButton" Click="Remove_Click" Background="#FFB83D48" BorderBrush="#FFD15C66" Content="Remove ELKA Network rules"/>
            <Button x:Name="ListButton" Click="List_Click" Background="#FF535D6B" BorderBrush="#FF697585" Content="List Network rules"/>
        </WrapPanel>

        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBox x:Name="SearchBox" TextChanged="SearchBox_TextChanged" ToolTip="Search by application name or executable path"/>
            <TextBlock x:Name="CountText" Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource SecondaryTextBrush}"/>
        </Grid>

        <Border Grid.Row="3" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6">
            <DataGrid ItemsSource="{Binding Applications, RelativeSource={RelativeSource AncestorType=UserControl}}"
                      Background="{DynamicResource SurfaceBrush}" Foreground="{DynamicResource PrimaryTextBrush}"
                      AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" CanUserReorderColumns="False"
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="{DynamicResource GridLineBrush}"
                      BorderThickness="0" RowHeaderWidth="0" RowHeight="48">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                        <Style.Triggers><Trigger Property="IsSelected" Value="True"><Setter Property="BorderBrush" Value="{DynamicResource SelectionBrush}"/><Setter Property="BorderThickness" Value="1"/></Trigger></Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Width="48" Binding="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                        <DataGridCheckBoxColumn.Header><CheckBox x:Name="SelectAllCheckBox" Click="SelectAll_Click" ToolTip="Select or clear all visible applications"/></DataGridCheckBoxColumn.Header>
                    </DataGridCheckBoxColumn>
                    <DataGridTextColumn Header="Application" Width="2*" IsReadOnly="True" Binding="{Binding DisplayName}"/>
                    <DataGridTextColumn Header="Executable path" Width="5*" IsReadOnly="True" Binding="{Binding ExecutablePath}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <TextBlock x:Name="StatusText" Grid.Row="4" Margin="0,10,0,0" TextWrapping="Wrap"
                   Foreground="{DynamicResource SecondaryTextBrush}"
                   Text="Search for applications, select one or more, then choose ports, protocol, direction, and firewall profiles."/>
    </Grid>
</UserControl>
